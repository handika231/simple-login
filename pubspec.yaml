name: simple_login
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.18.1 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  lottie: ^1.4.3
  google_fonts: ^3.0.1
  provider: ^6.0.3
  get_it: ^7.2.0
  dio: ^4.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  mockito: ^5.3.1
  flutter_lints: ^2.0.0
  build_runner: ^2.2.1

flutter:
  uses-material-design: true
